# Copy to .env and adjust values for your environment.
# Keep secrets out of version control.

APP_NAME=template-app
APP_ENV=development
APP_PORT=8000

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=template_db
DB_USER=developer
DB_PASSWORD=change-me

# Third-party services (replace or remove as needed)
CACHE_URL=redis://localhost:6379/0
API_BASE_URL=https://api.example.com

# Add new variables when features require them and document the changes in docs/ci-setup.md.
